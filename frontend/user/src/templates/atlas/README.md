# Atlas storefront template

Structured catalogue storefront skin. It is selected with `storefront_template: "atlas"` and reuses the existing customer-facing routes, carts, payments, and account views.
