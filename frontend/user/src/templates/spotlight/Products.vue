<template><div class="spotlight-products min-h-screen bg-[#f4f1ea] px-4 py-12"><div class="mx-auto max-w-6xl"><div class="mb-8 flex items-end justify-between border-b border-stone-300 pb-5"><h1 class="text-4xl font-black">{{ t('nav.products') }}</h1><RouterLink to="/" class="text-sm font-bold text-violet-700">{{ t('nav.home') }}</RouterLink></div><div class="space-y-3"><RouterLink v-for="p in products" :key="p.id" :to="`/products/${p.slug}`" class="flex items-center justify-between border-b border-stone-300 py-5 transition hover:px-3 hover:bg-white"><div><span class="mr-4 text-sm text-stone-400">{{ String(p.id).padStart(2,'0') }}</span><span class="text-xl font-bold">{{ getLocalizedText(p.title) }}</span></div><span class="text-sm font-bold text-violet-700">{{ t('blog.readMore') }} -></span></RouterLink></div></div></div></template>
<script setup lang="ts">import { onMounted, onUnmounted } from 'vue'; import { useI18n } from 'vue-i18n'; import { useProductList } from '../../composables/useProductList'; import { useLocalized } from '../../composables/useProduct'; const {t}=useI18n(); const {getLocalizedText}=useLocalized(); const {products,initialize,cleanup}=useProductList({pageSize:30,homeRouteName:'products'}); onMounted(()=>void initialize()); onUnmounted(cleanup)</script>
